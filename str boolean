package string;

public class namestr {
    public static void main(String[]args){
        String s="vinothini";
        // length of the string
        int len=s.length();
        System.out.println("Length:"+s.length());
// charAt()->access a specific char
char ch=s.charAt(2);
System.out.println("access the char:"+ch);
// substring(bigin index,end index+1)-> to find subsrting
String substr=s.substring(0,4);
System.out.println("substring:"+substr);
// to upper case
System.out.println("upper case:"+s.toUpperCase());
// to lower case
System.out.println("lowercase:"+s.toLowerCase());
//-------compare------
String name="abinaya"; 
boolean isSame=name.equals("vinothini");
System.out.println("same compare:"+isSame);
boolean isSameCase=name.equalsIgnoreCase( "abinaya");
System.out.println("without case senstive:"+isSameCase);





        
    }
    
}
